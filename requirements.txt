# DOI AI 모델 관리 에이전트 — 의존성
# 라이선스: 전부 Apache-2.0 / BSD / MIT (AGPL/GPL/NC 금지)

# ── 딥러닝 프레임워크 ──
torch>=2.5.0
torchvision>=0.20.0

# ── 세그멘테이션 (Apache-2.0) ──
mmsegmentation>=1.2.0
mmengine>=0.10.0
mmcv>=2.1.0

# ── 백본 / 프리트레인 가중치 (Apache-2.0) ──
timm>=1.0.0

# ── 부트스트래핑 라벨링 (Apache-2.0) ──
groundingdino-py>=0.5.0
segment-anything-2>=1.0

# ── 객체 탐지 (RT-DETR은 git clone 사용) ──
# RT-DETR: git clone https://github.com/lyuwenyu/RT-DETR.git → rtdetrv2_pytorch/

# ── 변화 탐지 (MIT, git clone) ──
# BIT-CD: git clone https://github.com/justchenhao/BIT_CD.git

# ── 수치 예측 (Apache-2.0) ──
xgboost>=2.0.0

# ── 강화학습 (MIT) ──
stable-baselines3>=2.3.0

# ── 데이터/실험 관리 ──
dvc>=3.50.0
mlflow>=2.15.0

# ── 라이선스 검증 ──
pip-licenses>=4.0.0

# ── 대시보드 백엔드 ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ── 인증 (JWT + bcrypt) ──
PyJWT>=2.8.0
bcrypt>=4.1.0

# ── 동시성 제어 ──
filelock>=3.15.0

# ── 추론 런타임 ──
onnxruntime>=1.18.0
onnx>=1.16.0

# ── 지리 데이터 처리 ──
rasterio>=1.3.0
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
fiona>=1.9.0

# ── HTTP 클라이언트 (VWorld API 등) ──
requests>=2.31.0

# ── 3D / DSM (Open3D, MIT) ──
open3d>=0.18.0

# ── 이미지 처리 ──
numpy>=1.26.0
Pillow>=10.0.0
opencv-python>=4.9.0
scikit-image>=0.22.0

# ── 머신러닝 유틸 ──
scikit-learn>=1.4.0
pandas>=2.2.0
scipy>=1.12.0

# ── 산출물 생성 ──
openpyxl>=3.1.0
fpdf2>=2.7.0
reportlab>=4.0.0

# ── 테스트 ──
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ── 개발 도구 ──
ruff>=0.4.0
mypy>=1.10.0
